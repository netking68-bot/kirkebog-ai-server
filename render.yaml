services:
  - type: web
    name: kirkebog-ai
    env: python
    plan: free
    buildCommand: "apt-get update && apt-get install -y tesseract-ocr tesseract-ocr-dan && pip install -r requirements.txt"
    startCommand: "python server.py"
    autoDeploy: true
